apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brilliantapp-ingress
spec:
  rules:
    - host: brilliantapp.zad
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: brilliantapp
                port:
                  number: 80
